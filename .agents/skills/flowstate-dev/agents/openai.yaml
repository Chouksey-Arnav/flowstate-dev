interface:
  display_name: "Flowstate Dev"
  short_description: "Repo-specific patterns and workflows for flowstate-dev"
  default_prompt: "Use the flowstate-dev repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true